<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Config 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.animalConnect.mapper.ProMapper">

	 <select id="proList" resultType="com.animalConnect.domain.ProVO"> 
	 <![CDATA[
		SELECT PRONAME,PROEMAIL, PROPW, PROHP, PROCHECK, NICNAME,PROCHECKNUM FROM 
			(SELECT /*+ INDEX_DESC(PRO PROEMAIL_PK) */ ROWNUM RN,PRONAME,PROEMAIL, PROPW, PROHP, PROCHECK, NICNAME,PROCHECKNUM	
			 FROM PRO
			 WHERE ROWNUM <= #{pageNum} * #{amount})
		WHERE RN > (#{pageNum} - 1) * #{amount}
	]]>
	 </select>
	 
	<select id="proMapList" resultType="string">
		SELECT PROMAPS FROM "MAP" 
		WHERE PROEMAIL = #{proEmail}
	</select>
	<select id="totalCnt" resultType="_int">
		SELECT COUNT(*) FROM PRO
	</select>
	<select id="checkProId" resultType="_int">
		SELECT COUNT(*) FROM PRO WHERE PROEMAIL = #{id}
	</select>
	<select id="proCheck_num" resultType="_int">
		SELECT PROCHECKNUM FROM PRO WHERE PROEMAIL=#{proEmail}
	</select>
	
	<select id="proApproveList"  resultType="com.animalConnect.domain.ProVO">
		SELECT * FROM PRO WHERE PROCHECK='NO'
	</select>
	
	<update id="proApproveOk">
		UDPATE PRO
		SET PROCHECK = 'YES'
		WHERE PROEMAIL = #{proEmail}
	</update>
	
	
	<insert id="proJoin">
		INSERT INTO pro(PRONAME,PROEMAIL,PROPW,PROHP,NICNAME,PROCHECK) 
		VALUES(#{proName},#{proEmail},#{proPw}, #{proHp},#{nicName},'NO')
	</insert>
	<insert id="proMapRegister">
		INSERT INTO "MAP" 
		VALUES(#{proEmail},#{proMaps})
	</insert>
	<select id="proLogin" resultType="string">
		SELECT NICNAME FROM PRO WHERE PROEMAIL = #{id} AND PROPW = #{pw}
	</select>
	
	
</mapper>